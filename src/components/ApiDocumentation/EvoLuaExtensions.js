import React from 'react';

import CategoryOverviewSection from "./CategoryOverviewSection";

export default function EvoLuaExtensions() {
	return (
		<CategoryOverviewSection
			heading="Evo.lua API"
			text="Additional high-level APIs for many tasks are implemented in global namespaces. There's some nonstandard extensions to Lua APIs, too."
		/>
	);
  }